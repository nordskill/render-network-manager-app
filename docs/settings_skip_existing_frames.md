# Skip Existing Frames

Skip Existing Frames prevents the app from downloading frames that are already present in the selected download folder.

## When to use it

Keep this enabled when resuming interrupted downloads or downloading into a folder that already contains completed frames. The app checks what is already there and focuses only on missing files.

## Good to know

If you want to replace files that already exist locally, turn this setting off before downloading again.